apiVersion: v1
kind: Service
metadata:
  name: docker-kubernetes-web-app-service
spec:
  type: LoadBalancer
  selector:
    app: docker-kubernetes-web-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
